{
  "name": "home-teacher-meta",
  "version": "1.0.0",
  "private": true,
  "description": "HomeTeacher meta-repository for integrated build and deployment",
  "scripts": {
    "build:drawing-common": "pnpm --filter @thousands-of-ties/drawing-common build",
    "dev": "pnpm --filter tutotuto dev",
    "dev:discuss": "pnpm --filter tutotuto dev:discuss",
    "build:kids": "pnpm --filter tutotuto build:kids",
    "build:discuss": "pnpm --filter tutotuto build:discuss",
    "build:all": "pnpm --filter tutotuto build:all"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.3.0"
  }
}